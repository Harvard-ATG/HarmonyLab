define([
	'lodash', 
	'app/components/music'
], function(
	_, 
	MusicComponent
) {

	describe('MusicComponent', function() {
		it('should create a component', function() {
			var settings = {chords:null,keySignature:null};
			//var component = new MusicComponent(settings);
			//component.init(null);
			expect(true).toBe(true);
			//expect(component.settings).toBe(settings);
			//expect(component.parentComponent).toBe(null);
			//expect(component.components).toBeTruthy();
		});
	});
});
