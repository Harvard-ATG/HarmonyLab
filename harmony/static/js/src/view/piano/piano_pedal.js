/* global define: false */
define(['jquery','lodash'], function($, _) {
	"use strict";

	var PianoPedal = function(index) {
		this.index = index;
	};

	_.extend(PianoPedal.prototype, {

	});

	return PianoPedal;
});
